
describe('The `characters` reducer', function () {
  it('should return the intital state');

  it('should return the current state for undefined actions');

  it('should handle RECEIVE_CHARACTERS');
});
